  export const TOPICS_LIST = (state, topics) => {
    state.topics = topics
  }
  export const increment =  state => {
    state.count++
  }
  export const decrement = state => {
    state.count--
  }